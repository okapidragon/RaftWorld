open in vscode for readability!!!


circular dependency of playerLogic, taskLogic?

-------------------------------------(imports.py:)-------------------------------------
displayOutput()
class OutputMessage ()
    __init__
    append(message)

currentEvent
Progression Variables -> fishUnlock, seaweedUnlock, displayedResources, displayedTasks


-------------------------------------(inventoryLogic.py:)-------------------------------------
imports.py as imp

inventoryUpdate() -> loops through displayedResoirces and displayed

class Resource (name, quantity, food=False, hungerScore = 100)
    all = []
    __init__
    add(amount)
    remove(amount)
    __str__

class Item (Resource, breakable=False, breakable=0.0)
    __init__
    tryBreak() -> trys to break the resource upon use

lookup(name) -> returns Resource/Item with that name
-> Items are usables like Fishing Rod
-> Resources are Wood, Fish, Seaweed, etc.
-> Only one resource instance of each object

class CraftingRecipe (name, cost, neededItems, result, time = 2) -> very similar to Task
    __init__
    able() -> returns True if you have the resources otherwise False
    async craft() -> crafts self async. 
        Checks if able
        Removes from inventory
        Async Waits
        Try Breaking Needed Items
        Gives you result and adds to output

-------------------------------------(taskLogic.py)-------------------------------------
imports.py as imp
inventoryLogic.py as inv
playerLogic.py as pl

initializeSeaweed() -> Adds task and inventory regarding seaweed. Displays a message

class Task (name, cost, neededItems, time, reward, inputs = None)
    all = []
    __init__
    able() -> returns if you are able to create the class
    async execute() -> does the task
        Checks if doing something
        Checks if able
        Removes Cost
        async wait time
        Checks if you fished for the first time
        Breaks all items and deals with seaweed
        Stops doing task
        If the reward is a dict, run through giving rewards and outputting dialogue
        If the reward is a function, do that function taking in the inputs.
    
lookup(name) -> finds the task with the given name
async runTask(task, button) -> Runs the task and handles changing the color of the button
taskButtonUpdate() -> displays and updates all tasks in displayed tasks

-------------------------------------(playerLogic.py)-------------------------------------
imports.py as imp
inventoryLogic.py as inv
taskLogic.py as task

setBarProgress(value) -> sets the hunger bar to a hunger value
displayBoatSize(boat) -> displays the boat size of a boat instance
displayTime(current_time): -> displays the current time

async resizeBoat() -> called by button and handles everything to do with resizing boat in two seconds. Creates a task instance but deletes from all tasks


class Boat (durability, size)
    all = []
    __init__
    changeSize(newSize) -> handles the reward function for the class

class Player (name, hunger = 100, task = None)
    __init__
    eat(foodResource)
    hungerFrame()

class gameTime()
    all = []

    __init__
    advance(increment) -> advances the time in hours and minutes and days based on a minute increment.
